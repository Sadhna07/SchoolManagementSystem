<%= form_for(@class_info) do |f| %>
  <% if @class_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_info.errors.count, "error") %> prohibited this class_info from being saved:</h2>

      <ul>
      <% @class_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :class_id %><br>
    <%= f.number_field :class_id %>
  </div>
  <div class="field">
    <%= f.label :class_name %><br>
    <%= f.text_field :class_name %>
  </div>
  <div class="field">
    <%= f.label :maximum_limit %><br>
    <%= f.number_field :maximum_limit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
